{
  "$schema": "../gen/schemas/capabilities.schema.json",
  "identifier": "default",
  "description": "Default capability for mailbrus-desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-spawn",
    "shell:allow-kill"
  ]
}
